{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Post",
  "description": "JSON Schema for a single JSONPlaceholder post resource",
  "type": "object",
  "required": ["userId", "id", "title", "body"],
  "additionalProperties": false,
  "properties": {
    "userId": {
      "type": "integer",
      "minimum": 1,
      "description": "Identifier of the user who authored this post"
    },
    "id": {
      "type": "integer",
      "minimum": 1,
      "description": "Unique post identifier, server-assigned"
    },
    "title": {
      "type": "string",
      "minLength": 0,
      "description": "Short descriptive title of the post"
    },
    "body": {
      "type": "string",
      "minLength": 0,
      "description": "Full content / body of the post"
    }
  }
}
